#!/bin/sh
# ---------------------------------------------------------
# -- Starts MySQL server
# ---------------------------------------------------------

echo "o MySQL Server is about to start.."
#/usr/sbin/mysqld -h localhost -u root
#/usr/sbin/mysqld -h localhost -u root --console
sudo service mysql start

echo "o MySQL Server is likely started."
# Check if started:
# sudo netstat -tap | grep mysql

