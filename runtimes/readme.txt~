Title: Config samples
Inception: 2014-01-15

This folder contains snapshots of Data structure manipulation commands for different backends.
Folders:
 - sql_db_samples: for SQL DBs
 - javadb_sample_runtime: 
     Sample runtime to create and run derby instance. 
     It's advised to copy it to folder outside VCS tree(or add it to VCS ignore list e.g. .gitignore). 

	 
TODO: 
 1. Add NoSQL (MongoDB,Solr,Cassandra,Neo4j).
