Helping commands for solr handling(albeit very primitive and not production Ops ready yet).

To start solr: start_solr. 
To load initial data: load_data (don't forget to start solr before).

NOTE: all of them can be used just as guidance of own config/setup process.