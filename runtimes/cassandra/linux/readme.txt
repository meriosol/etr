Helping commands for Cassandra handling(albeit very primitive and not production Ops ready yet).

To start server: sudo ./start_cass.sh. 
To start client: ./client.sh (don't forget to start server before).

NOTE: all of them can be used just as guidance of own config/setup process.
